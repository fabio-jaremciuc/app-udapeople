---
- name: Starting PM2
  shell: |
    cp /home/backend.tar.gz /home/ubuntu/backend.tar.gz
    cd /home/ubuntu/
    tar xzvf backend.tar.gz
    mv home/circleci/project/backend .
    cd backend
    sudo npm install
    sudo npm run build
    sudo pm2 start npm --name backend -- start